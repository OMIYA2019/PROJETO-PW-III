@model ProjetoOdontoClin.Models.modelLogin
@{
    ViewBag.Title = "consBuscaLogin";
    Layout = "";
}
<h2>consBuscaPaciente</h2>

@using (Html.BeginForm("consBuscaLogin", "Login", FormMethod.Post))
{

<div>

    Código do Login
    @Html.EditorFor(x => x.usuario, new

{

   htmlAttributes = new { id = "txtCod", name = "txtCod", type = "text", @class = "form-control", placeholder = "Digite o usuario", required = "required", maxlength = "30" }
})

    @Html.ValidationMessageFor(x => x.usuario, "", new { @class = "text-danger" })

</div>

    <br /> <input type="submit" value="Buscar"><br />

}


@Html.Raw(@ViewBag.GridViewString)